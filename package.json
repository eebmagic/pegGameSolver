{
  "name": "peg_game_solver",
  "version": "1.0.0",
  "description": "Solver for Cracker Barrel peg game",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "echo 'starting build' && npm install",
    "start": "node app.js"
  },
  "author": "E Bolton",
  "license": "ISC",
  "devDependencies": {
    "cookie-parse": "^0.4.0",
    "cors": "^2.8.5",
    "express": "^4.17.1"
  }
}
